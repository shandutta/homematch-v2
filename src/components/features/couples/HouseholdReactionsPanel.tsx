'use client'

// Stub — HouseholdReactionsPanel removed during Clerk/Supabase migration.
// Reaction data pipeline needs Supabase household_reactions table (inactive).
// Re-implement when Supabase project is restored.
export function HouseholdReactionsPanel({
  propertyId: _propertyId,
  className: _className,
}: {
  propertyId: string
  className?: string
}) {
  return null
}
